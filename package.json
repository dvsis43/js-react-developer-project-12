{
  "name": "@hexlet/code",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run build --prefix hexlet-chat-server",
    "start": "start-server -s ./hexlet-chat-server/build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "cd hexlet-chat-server && npm ci"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@hexlet/chat-server": "^2.0.3"
  }
}
